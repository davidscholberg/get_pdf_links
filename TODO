* for recursive searches, ignore URLs that point to the current page

* for recursive searches, do breadth-first instead of depth-first

* check for duplicate URLs

* remove array that holds PDF URLs (memory hog)
